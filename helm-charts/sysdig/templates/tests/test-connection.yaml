apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sysdig.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "sysdig.name" . }}
    helm.sh/chart: {{ include "sysdig.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "sysdig.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
